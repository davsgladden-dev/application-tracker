spring:
  application:
    name: application-tracker

  datasource:
    url: jdbc:h2:file:~/.jobtracker/db
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8075